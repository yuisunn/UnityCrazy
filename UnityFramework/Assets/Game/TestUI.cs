using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using SLCGame.Unity;

public class TestUI : UIWndBase { 
	// Use this for initialization
	void Awake () {
        base.Awake();
    }

    public void Test()
    {
        //UIButton btnOK = null; 
        //btnOK.onClick = 

        //EventDelegate del = new EventDelegate();
        //del.target

        //UIEventListener.Get(gameObject).onClick

        Debug.Log("MainPanel.Test function Invoked!");
    }
}
